<!DOCTYPE html>
<html>
  <head>
    <style>
    * {
      font-family: helvetica;
    }
    #main-wrapper {
      width: 70%;
      max-width: 50em;
      margin: auto;
    }
    input[type=search] {
      width: 80%;
      margin-top: 10em;
      margin-bottom: 4em;
    }
    b {
      color: rgb(237, 93, 64);
    }
    p {
      margin: .5em 0 .5em 0;
    }
    .result {
      padding: 1em;
    }
    .result:nth-child(even) {
      background: rgb(251, 251, 251);
    }
    </style>
    <meta charset="UTF-8">
    <title>STFS</title>
  </head>
  <body>
    <div id="main-wrapper">
      <div id="search-wrapper">
        <form>
          <input autofocus type="search" name="q" placeholder="Search The Tim Ferriss Show..." value="<%= @query %>">
          <button type="submit">Search</button>
        </form>
      </div>
      <% if @episodes.empty? && !@query.nil? %>
        <h2><%= "There are no search results for '#{@query}'"%></h2>
      <% else %>
        <div id="results-wrapper">
          <% @episodes.each do |episode| %>
            <div class="result">
              <h3><%= episode.title %></h3>
              <p><%= episode.description %></p>
              <a href="<%= episode.url %>" target="_blank">original blogpost ⬈</a>
            </div>
          <% end %>
        </div>
      <% end %>
    </div>
  </body>
</html>
